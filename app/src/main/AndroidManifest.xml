<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.estruch18.proyecto5ead" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".CustomContentProvider"
            android:authorities="com.example.estruch18.proyecto5ead" />

        <activity
            android:name=".InsertarEstudiante"
            android:label="@string/title_activity_insertar_estudiante" >
        </activity>
    </application>

</manifest>
